name: Raw Data Async Sync (Legacy Disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  legacy_disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy workflow notice
        run: |
          echo "raw-data-async is deprecated and intentionally disabled."
          echo "Use scripts/run_raw_pipeline_local.sh for raw ingestion."
